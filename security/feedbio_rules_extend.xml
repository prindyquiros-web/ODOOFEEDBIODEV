<odoo>
  <data noupdate="1">

    <!-- Contratos -->
    <record id="rule_x_contrato_multi_company" model="ir.rule">
      <field name="name">Contratos por empresa</field>
      <field name="model_id" ref="model_x_contrato"/>
      <field name="domain_force">[('company_id', 'in', user.company_ids.ids)]</field>
      <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Órdenes de Pedido -->
    <record id="rule_x_orden_pedido_multi_company" model="ir.rule">
      <field name="name">Órdenes de pedido por empresa</field>
      <field name="model_id" ref="model_x_orden_pedido"/>
      <field name="domain_force">[('company_id', 'in', user.company_ids.ids)]</field>
      <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Recepciones -->
    <record id="rule_x_recepcion_multi_company" model="ir.rule">
      <field name="name">Recepciones por empresa</field>
      <field name="model_id" ref="model_x_recepcion"/>
      <field name="domain_force">[('company_id', 'in', user.company_ids.ids)]</field>
      <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Adendas -->
    <record id="rule_x_contrato_adenda_multi_company" model="ir.rule">
      <field name="name">Adendas por empresa</field>
      <field name="model_id" ref="model_x_contrato_adenda"/>
      <field name="domain_force">[('company_id', 'in', user.company_ids.ids)]</field>
      <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Lotes -->
    <record id="rule_x_lote_multi_company" model="ir.rule">
      <field name="name">Lotes por empresa</field>
      <field name="model_id" ref="model_x_lote"/>
      <field name="domain_force">[('company_id', 'in', user.company_ids.ids)]</field>
      <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Campañas -->
    <record id="rule_x_campana_multi_company" model="ir.rule">
      <field name="name">Campañas por empresa</field>
      <field name="model_id" ref="model_x_campana"/>
      <field name="domain_force">[('company_id', 'in', user.company_ids.ids)]</field>
      <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Documentos de Recepción -->
    <record id="rule_x_recepcion_documento_multi_company" model="ir.rule">
      <field name="name">Documentos de recepción por empresa</field>
      <field name="model_id" ref="model_x_recepcion_documento"/>
      <field name="domain_force">[('company_id', 'in', user.company_ids.ids)]</field>
      <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

  </data>
</odoo>
